# Model
model_name: "amuvarma/8b-2m-pretrain"  # Replace with your base model must be compatible with the tokenizer and transformers library
tokenizer_name: "meta-llama/Llama-3.2-3B"

# Training Args
epochs: 3
batch_size: 1
number_processes: 1
pad_token: 128263
save_steps: 5000
learning_rate: 5.0e-6

# Datasets
TTS_dataset: "amuvarma/400-playful-conversations-iids"

# Naming and paths
save_folder: "checkpoints"
project_name: "personality-tune"
run_name: "8b-16k-base"

resize_dataset: true